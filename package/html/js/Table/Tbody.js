import BaseContainer from "../Base/Base.js";


// TableBodyContainer
export default class TableBodyContainer extends BaseContainer {

    constructor(parentContainer) {
        super("tbody", parentContainer);
    }

}