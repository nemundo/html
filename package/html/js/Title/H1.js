import ContentContainer from "../Base/Content.js";

export default class H1Container extends ContentContainer {

    constructor(parentContainer = null) {
        super("h1", parentContainer);
    }

}