import ContentContainer from "../Base/Content.js";


export default class H4Container extends ContentContainer {

    constructor(parentContainer=null) {
        super("h4", parentContainer);
    }

}