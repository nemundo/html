import ContentContainer from "../Base/Content.js";
import BaseContainer from "../Base/Base.js";

export default class FooterContainer extends BaseContainer {

    constructor(parentContainer) {

        super("footer", parentContainer);

    }

}