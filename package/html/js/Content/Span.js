import ContentContainer from "../Base/Content.js";

export default class SpanContainer extends ContentContainer {

    constructor(parentContainer) {

        super("span", parentContainer);

    }

}