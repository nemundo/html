import ContentContainer from "../Base/Content.js";

export default class StrongContainer extends ContentContainer {

    constructor(parentContainer) {

        super("strong", parentContainer);

    }

}
